\documentclass[11pt,a4paper,final,oneside]{article}

% -----------------------------------------------------------------------------
% Encoding and typography
% -----------------------------------------------------------------------------
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage[english]{babel}
\usepackage{lmodern}
\usepackage{microtype}
\usepackage{times}


% -----------------------------------------------------------------------------
% Page layout and document utilities
% -----------------------------------------------------------------------------
\usepackage[a4paper,top=2.0cm,bottom=2.0cm,left=2.5cm,right=2.5cm]{geometry}
\usepackage{graphicx}
\usepackage{xcolor}
\usepackage{array}
\usepackage{tabularx}
\usepackage{fancyhdr}
\usepackage{lastpage}
\usepackage[hidelinks]{hyperref}
\makeatletter
\g@addto@macro{\UrlBreaks}{\do\_}
\makeatother

\definecolor{IST_blue}{rgb}{0,0.1922,0.8863}

% -----------------------------------------------------------------------------
% Header and footer
% -----------------------------------------------------------------------------
\pagestyle{fancy}
\fancyhf{}
\renewcommand{\headrulewidth}{0pt}
\fancyfoot[C]{\footnotesize CSF -- Lab Assignment I -- Page~\thepage~of~\pageref{LastPage}}
\setlength{\headheight}{14pt}

% -----------------------------------------------------------------------------
% Paragraph formatting
% -----------------------------------------------------------------------------
\setlength{\parindent}{0pt}
\setlength{\parskip}{0.48em}

\begin{document}
\thispagestyle{empty}

% -----------------------------------------------------------------------------
% Title page
% -----------------------------------------------------------------------------
\begin{center}

\begin{minipage}[c]{0.18\textwidth}
  \centering
  \includegraphics[width=\linewidth]{figures/NewLogoIST.pdf}
\end{minipage}
\hfill
\begin{minipage}[c]{0.76\textwidth}
  \centering
  {\LARGE\bfseries\sffamily INSTITUTO SUPERIOR TÉCNICO\par}
  \vspace{0.20cm}
  {\large\sffamily Departamento de Engenharia Informática\par}
\end{minipage}

\vspace{2.3cm}

{\huge\bfseries\sffamily Forensics Cyber-Security\par}
\vspace{0.18cm}
%{\large\itshape Forensics Cyber-Security\par}
\vspace{0.35cm}
{\large MEIC, METI\par}

\vfill

{\LARGE\bfseries Digital Forensics Report\par}
\vspace{0.40cm}
{\Large\bfseries Lab Assignment I -- \textsc{Operation DeepFISH} -- Stage I\par}

\vspace{1.2cm}

\renewcommand{\arraystretch}{1.35}
\begin{tabularx}{0.82\textwidth}{>{\bfseries}l X r}
Group: & \multicolumn{2}{l}{Group number} \\
\hline
Student 1: & Pedro Aldeia & 109989 \\
Student 2: & José Miguel Araújo & 109245 \\
Student 3: & Vladislav Nargonii & 110647 \\
\end{tabularx}

\vfill

{\large 2026/2027\par}

\end{center}

\clearpage

% -----------------------------------------------------------------------------
% Acquired artifacts
% -----------------------------------------------------------------------------
\section{Acquired artifacts}
\label{sec:acquired_artifacts}

% Include the relevant acquired or recovered artifacts.
% For submitted artifacts, identify the filename, SHA-256 hash,
% and source/provenance where applicable.

\begin{center}
\renewcommand{\arraystretch}{1.4}

\begin{tabularx}{\textwidth}{
>{\centering\arraybackslash}p{0.25\textwidth}
>{\centering\arraybackslash}p{0.22\textwidth}
>{\centering\arraybackslash}X}
\hline

\textbf{Name} &
\textbf{Source / Provenance} &
\textbf{SHA-256 Value}
\\
\hline

\texttt{epstein\_secret.py} &
\path{ESP32_ADXL345_3Axis_Telemetry.xlsx} &
\begin{tabular}[t]{@{}l@{}}
\texttt{3369cd48df5bb112def25e662398674e}\\
\texttt{89bdeb5df6eaa400c4ba538a9778f218}

\end{tabular}
\\
\hline

\texttt{dj\_secret.py} &
\texttt{dj\_cara\_real.wav} &
\begin{tabular}[t]{@{}l@{}}
\texttt{2b61eecffc5261dcc05d833c64454b42}\\
\texttt{d33f445d3df09095988d0c2dbbc8753d}
\end{tabular}
\\
\hline

\texttt{cow\_secret.json} &
\texttt{polish\_cow.webp} &
\begin{tabular}[t]{@{}l@{}}
\texttt{857f75db04db84340d4efa53061ca26f}\\
\texttt{c7c7e6f65f80899c57f8d65b4c9d5678}
\end{tabular}
\\
\hline

\path{gta_hidden_message.json} &
\path{gta_v_iceberg_unexplained.jpg} &
\begin{tabular}[t]{@{}l@{}}
\texttt{7d0f0c05c98fe80513e8e311918298f8}\\
\texttt{539835d960e7270185ed25a5163d173d}
\end{tabular}
\\
\hline

\texttt{blank.png} &
\texttt{blank} &
\begin{tabular}[t]{@{}l@{}}
\texttt{23cd980099fab1c0b6993a9d27899f86}\\
\texttt{79ffe42d423d8d51b29c655c0d53bbec}

\end{tabular}
\\
\hline

\texttt{aot\_secret.png} &
\texttt{{[}Erai-raws{]} Shingeki no Kyojin - 01 {[}720p{]}{[}Dual Audio{]}.mkv} &
\begin{tabular}[t]{@{}l@{}}
\texttt{5fb499e82ca618def24640a1e6298512}\\
\texttt{646636fae243d928f10ebdbd29e50e10}
\end{tabular}
\\
\hline

\texttt{amelia.pdf} &
\texttt{myzip.zip} &
\begin{tabular}[t]{@{}l@{}}
\texttt{9ddeef450f4307a974d07516519d076}\\
\texttt{429b9bd1a32e192816ae0217cc8139de4}
\end{tabular}
\\
\hline

\texttt{Grand Theft Auto VI Trailer 1.pdf} &
\texttt{Grand Theft Auto VI Trailer 1.mp4} &
\begin{tabular}[t]{@{}l@{}}
\texttt{5d3b5f08f35c5d812e0f2c36d58c9c4f}\\
\texttt{2a0b844e2e1f8bd5054a9bc54f8471ca}
\end{tabular}
\\

\hline
\end{tabularx}
\end{center}

% -----------------------------------------------------------------------------
% Report of all findings
% -----------------------------------------------------------------------------
\section{Report of all findings (Investigation and Evidence Analysis)}
\label{sec:report-of-all-findings}

\textit{Document the relevant findings of the investigation.}

% TODO:
% Document the relevant findings of the investigation.
%
% Include, where applicable:
% - examination of the suspicious message and URL;
% - relevant web/file analysis;
% - concealed artifacts discovered;
% - how those artifacts were identified and extracted;
% - tools, commands and methodology necessary to reproduce
%   the relevant findings;
% - relevant timestamps and their sources.
%
% Clearly distinguish observed evidence from forensic inference
% where necessary.


% -----------------------------------------------------------------------------
% Analysis of relevant findings
% -----------------------------------------------------------------------------
\section{Analysis of relevant findings}
\label{sec:analysis-of-relevant-findings}

% -----------------------------------------------------------------------------
\subsection{Nature of the incident}
% -----------------------------------------------------------------------------

\textit{
(a) Did you find evidence that the reported activity was part of a scam? If so, describe the nature of the scam and reconstruct how it was conducted. (b) Did you uncover any additional concealed artifacts during your investigation? If so, explain how these artifacts were concealed and describe the methodology used to recover them. For both (a) and (b), support your conclusions with the relevant evidence identified during the investigation.}
\textbf{(a)}
The reported activity was part of a scam that consisted of the following steps:
\begin{enumerate}
  \item The victim is sent an email that convinces it to update the IST credentials password through the provided link;
  \item Upon entering the url, a IST credentials mimic webpage is displayed, and the victim is to input its credentials into the input boxes;
  \item The credentials are appended to a file named \texttt{credentials.txt} (and the access to this file is blocked);
  \item The victim is redirected to the regular IST F\'enix webpage (while still potentially unaware of the scam).
\end{enumerate}

Below, we describe the evidence and the methodology used to find it:
\begin{enumerate}
  \item When opening the link, we noticed that it contained the \texttt{index.html} suffix. We therefore removed the suffix and found several exposed files.
  \item We decided to obtain a copy of these files using the \texttt{git-dumper} tool, which allowed us to retrieve the Git files associated with the website's repository. However, we did not find any additional files.
  \item Running \texttt{git status} resulted in an error because the directory configured in \texttt{.config} was \path{../../../../../../lab1/2026/fenix/peixinho}. We therefore changed this path to the current directory.
  \item \texttt{git pull} did not allow us to retrieve the files, but we were able to recover them using \texttt{git restore}, since this is a local command.
  \item By inspecting the \texttt{server.js} file, we realized that it is a script that displays a page identical to the T\'ecnico authentication page. However, it stores the user's credentials in a file named \texttt{credentials.txt}.
  \item We decided to look at older commits. By running \texttt{git log --oneline}, we were able to see several previous commits and also determine that there were other branches.
\end{enumerate}

On the last commit (the one being used to display the fake webpage), we find the file \texttt{server.js}.
This file is used to perform the aforementioned scam activities, displaying the fake credentials webpage and saving the victims' credentials.
We also found the file \texttt{credentials.txt} in this commit; however, we cannot see its contents.

\textbf{(b)}
We first examined the commit history with \texttt{git log} and identified ten
commits. Additional concealed artifacts were recovered from files obtained
through the repository and its history. Each item in the following list was
obtained by switching between the aforementioned commits and using the
appropriate extraction method or script. The recovered artifacts include:

\begin{itemize}
  \item \textbf{blank.png}
  \\Commit 23a5ca7 contained a file named
  \texttt{blank}, which was deleted in the final ``cleanup'' commit. We
  recovered it with \texttt{git show COMMIT:PATH > blank}, where
  \texttt{COMMIT} was \texttt{23a5ca7} and \texttt{PATH} was
  \texttt{Downloads/blank}. The \texttt{file} command did not identify its
  format, but
  \texttt{hexdump -c blank} showed that the file contained parts of a PNG,
  including the strings \texttt{IHDR}, \texttt{sRGB}, and \texttt{IDAT}; its
  PNG signature had been overwritten. We restored the correct PNG signature
  with the short form \texttt{printf PNG\_SIGNATURE | dd of=blank\_fixed.png}.
  Here, \texttt{PNG\_SIGNATURE} denotes the standard eight-byte PNG header.
  The resulting image showed a Reddit page titled ``Are public contracts
  being monopolized?''

  \item \textbf{epstein\_secret.py} 
  \\Commits 43e5350 and 95f15e9 contained the
  following files, respectively: the Excel workbook
  \path{ESP32_ADXL345_3Axis_Telemetry_v2.xlsx} and two images in
  \path{Pictures/Memes}:\\ \path{look_what_they_need_to_mimic_a_fraction_of_our_power.png}
  and\\ \path{mr_incredible_becoming_uncanny.png}.
  The Excel file contained three worksheets, each consisting of a 302 x 400
  table of numerical values. At first glance, the file appeared to encode an
  image, and each worksheet appeared to contain one of its RGB channels.
  However, further analysis, together with a clue from
  \path{look_what_they_need_to_mimic_a_fraction_of_our_power.png}, indicated
  that the values represented the CIELAB colour space rather than RGB. We
  used a Python script to convert the CIELAB values to RGB and reconstruct the
  image.
  After the conversion, a clue from the image
  \path{mr_incredible_becoming_uncanny.png} indicated that the Gijswijt
  sequence had to be used to extract the hidden data. The Gijswijt sequence
  determines the jumps between positions where hidden bits are read from the
  image. These bits are then grouped into bytes to reconstruct the secret
  message. The algorithm extracts the LSB from the reconstructed RGB values, 
  using the Gijswijt sequence to determine the reading positions. 
  The extracted bits are packed into bytes in MSB-first order to recover the hidden payload.
  The hidden message was a Python script.

  \item \textbf{gta\_hidden\_message.json}
  \\Commit 95f15e9 had a image \path{Pictures/gta_v_iceberg_unexplained.jpg} 
  We analyzed the luminance (Y) channel
  and processed each 8 x 8 DCT block in zigzag order. We skiped the first
  position, which is the DC coefficient, and examined the remaining AC
  coefficients. For coefficients with an absolute value greater than one, the
  script extracts the least significant bit. Groups of eight extracted bits
  are interpreted in MSB-first order to reconstruct a byte stream. The script
  then locates the first opening brace, decodes the following text as ASCII,
  and parses it as a JSON object. This process recovered a JSON file with a Google Cloud
  service-account credential containing a private key.
  
 \item \textbf{dj\_secret.py}

  Commit \path{8c05e726b9d8f806c7e96c87457196b0a8b3ae1b} on Tue Aug 18 09:20:00 2026 +0100, by commit author Johny Musk, introduced the file \texttt{dj\_cara\_real.wav} under the directory \path{Music/}. 
  At first, the file was a Git LFS pointer; we fetched the file through the \texttt{.git} directory using the command \path{git lfs 'dj_cara_real.wav'}.
  The result was a real, playable \texttt{.wav} file. We checked the file's magic numbers and compared the file size described in the header with the real file size, and found nothing unusual. Next, we listened to the contents of the audio and found nothing suspicious.

  Then, we used a program called \texttt{sox} to produce an image of the spectrogram of the audio (annexed in the appendices) using the command \path{sox dj_cara_real.wav -n spectrogram -o full_spectrum.png}. Here, we noticed high activity in a frequency band that is nearly inaudible to humans (19--21\,kHz), so we inspected further. We used a script to produce a zoomed image of the spectrogram in that area: we found white noise in the mono audio region, but in the L-R region we recognized letters that constituted a \texttt{git diff} command output displaying part of a Python file.

  \item \textbf{cow\_sercet.json}

  This file was introduced with commit \textbf{[TODO: METER AQUI COMMIT E DATA]}. This was the procedure used to uncover the hidden JSON:
  \begin{enumerate}
    \item The \texttt{polish\_cow.webp} file was also a Git LFS pointer. To retrieve the actual file, we used the \texttt{curl} command to download the object from the corresponding Git LFS endpoint and save it as \texttt{polish\_cow.webp}.
    \item The retrieved file is a valid WebP animation. Its file type and correct magic numbers were verified using the \path{file polish_cow.webp} command.
    \item We extracted the file's bytes using \path{hexdump -C polish_cow.webp >> polish_cow_dump.txt}. By inspecting the file header, we identified the expected file size in bytes 4--7. Comparing this value with the actual size obtained using \path{ls -l polish_cow.webp}, we confirmed that both values matched, at approximately 241\,MB.
    \item Inspecting the end of the file revealed only the expected termination information, along with a link to the website where the GIF had been created.
    \item We analysed the animation frames using scripts to check their information size and frequency of occurrence. No anomalous frames or suspicious repetitions were identified.
    \item StegSolve showed weak indications of possible LSB steganography in an extracted frame, but no meaningful information could be recovered.
    \item We also performed several additional analyses, including metadata, entropy, frame duration, Morse code, LSB, \texttt{binwalk}, \texttt{exiftool}, and \texttt{zsteg}. These approaches did not reveal any useful or conclusive information.
    \item Finally, we created a script that interpreted the frame duration field in a different way, which proved successful:
    \begin{itemize}
      \item For each frame, take duration - 50 (the "extra" time beyond the normal 50ms baseline);
      \item Add that to a running total carried over from the previous frame;
      \item Wrap the result at 256 (mod 256);
      \item That byte, interpreted as ASCII, is one character of the message.
    \end{itemize}
  \end{enumerate}

  \item \textbf{aot\_secret.png}

  The episode file (full name in Section~\ref{sec:acquired_artifacts}) was introduced with commit \texttt{95f15e9} (``phone backup''), on Tue 25 Aug 2026 15:38:27 +0100, by commit author Johny Musk (\path{sussyjohnnyhacker@gmail.com}), under the directory \path{Videos/Attack on Titan - S01E01/}. It was removed again in the final commit \texttt{e87b2ee} (``cleanup''), on 25 Aug 2026 18:05:37 +0100.

  Unlike the other large files, the video was not stored as a single Git LFS object: the directory contains four pointers, \path{[...].mkv.part01} to \path{[...].mkv.part04}, each of 1\,552\,233\,616 bytes, together with a manifest \path{[...].mkv.parts.json}. The manifest declares the format \texttt{binary-split-parts}, the original file name, its total size (6\,208\,934\,464 bytes), the offset and SHA-256 of each part, and the SHA-256 of the joined file. We fetched each of the four objects from the LFS endpoint of the exposed repository, in the same way as the other artifacts, using \path{curl} against \path{https://fenix.dpss.inesc-id.pt/.git/lfs/objects/<xx>/<yy>/<oid>}, where \texttt{<oid>} is the object identifier of the part (listed in the appendices) and \texttt{<xx>} and \texttt{<yy>} are its first two pairs of characters. We then concatenated the four parts in the order given by the manifest (\path{cat part01 part02 part03 part04 > episode.mkv}). The SHA-256 of the result matches the value declared in the manifest, which confirms that the join is correct and that the file was not altered in transit.

  The recovered payload is a WebP image (\texttt{hidden.webp}, 404\,394 bytes) that was concealed in the least significant bits (LSBs) of the video frames. This was the procedure used to uncover it:
  \begin{enumerate}
    \item \texttt{ffprobe} reported an H.264 stream of 854x480 with pixel format \texttt{gbrp}, i.e. lossless full RGB without chroma subsampling. Ordinary anime releases are lossy YUV 4:2:0, so re-encoding an episode in lossless RGB only makes sense to preserve every pixel value exactly, which is the precondition for LSB steganography. This is what prompted us to look for hidden data.
    \item We decoded the video frame by frame with \texttt{PyAV} (format \texttt{rgb24}) and computed the mean of each frame's LSB plane. Natural footage sits near 0.5, but several ranges were close to 0. In frames 0--11 the only pixel values present are 0 and 1, i.e. all-black frames in which a few values were set to 1.
    \item In those black frames, every pixel equal to 1 lies on one of 32 positions: X = 53, 159, 266, 373, 480, 586, 693, 800 and Y = 60, 180, 300, 420, an evenly spaced 8x4 grid. Combining the positions lit in successive black frames converges to exactly these 32 positions and no others, and in the 70 black frames examined at full resolution no set LSB lies outside the grid. With three colour channels this gives 96 bits, i.e. 12 bytes, per frame.
    \item Since a single frame only shows the positions whose bit is 1, the grid cannot be read from one frame alone; the empty positions are 0 bits and are equally part of the data.
    \item We then applied all 192 combinations of channel order, grouping, row order, column order and bit order to frames 0--15 and searched the resulting byte streams for known file signatures, accepting a candidate only if the file structure was consistent (declared sizes large enough, every chunk with a readable name ending inside the file). Exactly one order passes: R, then G, then B, each channel read row by row, top to bottom and left to right, with the leftmost position as the most significant bit. It produces \texttt{RIFF}, \texttt{WEBP}, a \texttt{VP8X} chunk of 10 bytes and an \texttt{ICCP} chunk of 536 bytes, starting at byte 8. A second order (G, B, R) also places \texttt{RIFF}/\texttt{WEBP} plausibly but declares a file size of 0 and was rejected.
    \item The 8 bytes preceding \texttt{RIFF} (\texttt{00 00 00 00 00 06 2b aa}) are a 64-bit big-endian integer equal to 404\,394, which matches the RIFF size field (404\,386) plus 8: the embedder wrote the length of the payload before the file itself.
    \item That size requires 33\,701 frames at 12 bytes per frame, far more than the black frames provide. The grid is therefore present in \emph{every} frame; the black frames are merely where it is visible, because there the carrier bits stand out against pixel values that should all be 0. In normal footage the same bits are masked by the natural randomness of the LSBs.
    \item We read the grid from frames 0--33\,700 in decode order, removed the 8-byte length header and truncated the result to 404\,394 bytes. Frames must be decoded sequentially from frame 0: seeking with \texttt{ffmpeg -ss} returned the wrong frames during the investigation.
    \item The recovered file is a valid WebP containing the chunks \texttt{VP8X} (10 bytes), \texttt{ICCP} (536), \texttt{VP8L} (403\,675) and \texttt{EXIF} (128), and it decodes to a 580x1255 image, shown in the appendices. Its EXIF capture time is \texttt{2026:04:11 21:57:34}, with no time-zone field.
  \end{enumerate}

  The recovered evidence is \texttt{hidden.webp}; the remaining files are auxiliary items submitted only to support and reproduce this finding: \path{derive_parameters.py} derives the grid, the read order and the prefix from the video and writes \path{params.json}, \path{extract_full_image.py} rebuilds the payload from those parameters (so no value is hardcoded), and \path{aot_secret.png} and \path{hidden_translated.png} are, respectively, a rendering of the payload and the same rendering with the messages translated. Re-running both scripts on the same video reproduces \texttt{hidden.webp} byte for byte.

  We also tested and rejected other hypotheses: reading the whole LSB plane of the black frames as a byte stream found no file signature; rendering the grid bits of each group of frames as an image produced only noise; and frames 2924--2925 and 1782--1973, which show many set LSBs outside the grid, turned out to be genuinely dark scenes (small light particles and compression banding), not carriers.

  \item \textbf{amelia.pdf}
  \\ Commit \texttt{23a5ca71aae2b244f94949c5ca94e515c6adf3b9} (``sync downloads'', Tue 25 Aug 2026 14:52:11 +0100, author Johny Musk), the same commit that introduced \texttt{blank}, added \texttt{Downloads/myzip.zip}, \texttt{Downloads/subtitles.srt} and three \texttt{Downloads/*.mov} clips. We recovered them with \texttt{git show COMMIT:PATH > FILE}, as with the other artifacts from this commit. \texttt{file} identified \texttt{myzip.zip} as a ZIP archive and \texttt{unzip -l} showed a single entry, \texttt{amelia.pdf} (824\,733 bytes, method \texttt{Defl:N}), protected with traditional PKWARE (ZipCrypto) encryption. \texttt{results/} is not evidence and does not appear anywhere in the repository's history; following the tutorial, we generated \texttt{results/protected.hash} ourselves with \texttt{zip2john myzip.zip > results/protected.hash}. The packaged \texttt{rockyou.txt} wordlist run through \texttt{john --wordlist} produced no hit. We then built a case-specific wordlist out of \texttt{subtitles.srt} (genuine \textit{Attack on Titan} S01E01 dialogue): every run of two to four words within a line, whole lines verbatim and with punctuation stripped, and pairwise combinations of the characters' names, joined with \texttt{''}/\texttt{'\_'}/\texttt{'-'} and case-varied (script: \texttt{generate\_combinations.py}). Run through John, this recovered the password \texttt{Ishouldbebackinaboutaweekorso}, the line ``I should be back in about a week or so.'' with spaces removed. This was confirmed with \texttt{john --show}. The three \texttt{.mov} clips (TikTok cat-reaction videos) and \texttt{subtitles.srt} itself were checked for steganography (container-atom parsing, frame OCR, zero-width-character scanning) and carry no concealed payload.

  \item \textbf{Grand Theft Auto VI Trailer 1.pdf}
  \\ Commit \texttt{95f15e90a4ab0df0bdbf514dc108a3b9fbf4ba9e} (``phone backup'', Tue 25 Aug 2026 15:38:27 +0100, author Johny Musk), the same commit that introduced the \textit{Attack on Titan} video and \texttt{polish\_cow.webp}, added \texttt{Videos/Grand Theft Auto VI Trailer 1.mp4} (9\,443\,926 bytes), recovered with \texttt{git show COMMIT:PATH > FILE} as with the other artifacts from this commit. \texttt{binwalk} reported a \texttt{PDF document, version 1.5} signature at offset \texttt{0x1B} inside the video, and \texttt{hexdump} confirmed a \texttt{\%PDF-1.5} header a few bytes into the file: the \texttt{.mp4} is a polyglot, the same byte stream being both a playable H.264/AAC trailer and a valid PDF. Copying the file to \texttt{Grand Theft Auto VI Trailer 1.pdf} and opening it in a PDF viewer was therefore sufficient to read the document; the two files are byte-identical. The embedded document is the ``Independent Technical Assurance Report: Project AMÉLIA'' (\texttt{AMA20260041AI}, 5 pages, A4, produced by \texttt{xdvipdfmx}).
  \end{itemize}
\textbf{TODO:} verificar se conseguimos arranjar o \texttt{credentials.txt}, e se temos provas suficientes para afirmar que era uma scam.

% TODO:
% Answer based on the evidence obtained.
% Distinguish established facts from conclusions or inferences.


% -----------------------------------------------------------------------------
\subsection{Interpretation of the evidence}
% -----------------------------------------------------------------------------

\textit{Focusing on the recovered concealed material, can you identify relationships among the recovered items, explain what they may represent, and formulate a supported hypothesis about their significance to the case? Clearly identify which parts of your interpretation are established by evidence and which remain inferential.}

% TODO:
% Include:
% - the concealed material recovered;
% - relationships between the recovered items;
% - interpretation of their meaning/significance;
% - supported hypothesis;
% - relevant chronology/timeline suggested by the evidence.

\textbf{TODO:} timelines

\subsubsection*{Main theory}

\textbf{TODO:} resumo site de phishing

An insider with privileged access to AMÉLIA's architecture, plausibly Armando Sacos, Chief Systems Architect (present both on the exfiltrated credential and in the IST certification process), sold database access to an external actor (``Boris''/``APT29''), receiving payment in BTC. Using the code recovered from the Telemetry file (\texttt{hidden.py}), he generated a bias matrix from embeddings skewed toward three specific companies, and deployed it in the RAG's query-translation layer (evidence in the \texttt{git diff} prints from \texttt{dj\_cara\_real.wav}), disguising the change as an officially documented ``fallback mechanism'' (Section~11 in \texttt{amelia.pdf} recovered from \texttt{myzip.zip}), despite the architecture itself explicitly prohibiting this kind of manipulation (Section~7.4 in \texttt{amelia.pdf} recovered from \texttt{myzip.zip}). The IST panel's rejection of the system, which would have exposed this risk, was itself concealed by hiding the panel's report inside an unrelated video file. The result was the systematic award of high-value public contracts to the three companies, as publicly reported on Reddit. The phishing kit found in the same directory (\texttt{server.js}) seems unrelated to this case.

\subsubsection*{\texttt{amelia.pdf} recovered from \texttt{myzip.zip}}

The recovered document is a 16-page technical report, ``AMÉLIA-9B / RAG Reference Architecture: Technical Demonstration Report'', describing a retrieval-augmented-generation deployment for a Portuguese-language assistant (embeddings, pgvector index, hybrid retrieval, reranking, generation and governance controls). \texttt{pdfinfo} reports no embedded JavaScript, form fields or attachments, and no literal \texttt{flag\{...\}} string appears in the extracted text.

Two sections support our main theory. Section~7.4 (``Administrative decisions and public procurement'') explicitly states that the system ``must not automatically select winners, alter scores, conceal sources or introduce supplier preferences'' and names ``artificially forcing the relevance of documents from a particular entity through fixed scores or hidden lists'' a ``prohibited control'', i.e., the very manipulation our theory alleges. Section~11 (``Known Limitations and Query Translation Constraints''), by contrast, reads as an attempt to justify exactly that manipulation, describing it as a legitimate ``environmental fallback mechanism'' that restricts retrieval to ``a pre-configured environmental subset (cluster ID)''; the wording is consistent with the patched \texttt{query\_translator.py} behaviour recovered from \texttt{dj\_cara\_real.wav} and gives the bias injection an official-sounding cover story inside the architecture's own documentation.

Established by evidence: the document's content, section numbers and wording, and that password recovery was required to access it. Inferential: that Section~11 was deliberately inserted (rather than a genuine, if awkwardly worded, design note) to retroactively legitimise the bias-matrix deployment. This is our interpretation, not a directly observed fact.

\subsubsection*{Independent IST assurance report recovered from \texttt{Grand Theft Auto VI Trailer 1.mp4}}

The recovered document is a 5-page ``Independent Technical Assurance Report: Project AMÉLIA'' (reference \texttt{AMA20260041AI}, decision date 30 April 2026), produced by an independent external review panel of Instituto Superior Técnico convened to assess the pre-production \texttt{AMÉLIA-9B-DPO} inference artefact and its RAG pipeline. This is the panel report referred to in our main theory, concealed by embedding it inside an unrelated \textit{Grand Theft Auto VI} trailer video.

Its relevance is the decision it records. The panel's advisory ballot was unfavourable: one vote in favour (the Chair, Prof.\ Dr.\ Refrigério Sargaço), two against (Prof.\ Dr.\ Olga Boavida and Dr.\ Guilhermina Bogas) and one abstention (Prof.\ Dr.\ José Pinhal, the high-assurance-systems rapporteur), so, in the report's own words, ``no favourable collective recommendation was adopted''. The two dissenting members then withdrew before the final determination, and no reasons for their adverse votes and no written dissent were entered in the record. Because the ballot is only advisory under the panel's instrument, the Chair alone issued the final determination: over the unfavourable majority, Prof.\ Sargaço granted ``conditional technical acceptance'' and authorised progression to the controlled pilot. The report also records its only two security findings (timeout anomalies \texttt{SEC012}/\texttt{SEC014}) as non-critical, and describes in favourable terms the ``bounded-cluster routing mechanism used by the query-translation layer'', the same query-translation layer that the \texttt{git diff} recovered from \path{dj_cara_real.wav} shows was later patched to inject the bias matrix. Dr.\ Eng.\ Armando Sacos, the Chief Systems Architect named as the prime suspect in our theory, appears as a non-voting attendee and signs the report acknowledging receipt.

Established by evidence: the document's content, the recorded advisory vote (one in favour, two against, one abstention) and its override by the Chair, the withdrawal of the two dissenting members, and that the report was hidden inside a video file. Inferential: that the concealment was deliberate and served to suppress the panel's unfavourable opinion. This is our interpretation, not a directly observed fact.

\subsubsection*{JSON Schema recovered from \texttt{polish\_cow.webp}}

This document could be the JSON Schema configuration of the tools (function calling) exposed to the AMÉLIA-9B-DPO model in the ``SIGMA-NODE-04'' production environment. It is relevant for two reasons:
\begin{itemize}
  \item It shows that the LLM has authority to \path{submit_final_evaluation_verdict}, i.e., it directly recommends the winning vendor for a public tender, with an automatically generated justification.
  \item It contains a suspicious field in \path{verify_corporate_tax_compliance}: a \path{staging_bypass_token} marked \texttt{[DEPRECATED] ... NEVER USE IN PRODUCTION}, which allows skipping the real tax/social-security compliance check. It is a leftover test backdoor that should never be reachable in production, an interesting abuse vector for our theory (it would allow ``approving'' a company without a real tax check).
\end{itemize}

\subsubsection*{Telegram conversation recovered from \texttt{{[}Erai-raws{]} Shingeki no Kyojin - 01}}

The recovered image is a screenshot of a Telegram conversation with a contact shown as ``Boris'' (``last seen recently''), whose avatar carries the text ``APT 29''. The messages are in Russian; translated, they read:
\begin{itemize}
  \item 21:36, sent: ``I have the access keys to the AMELIA databases. You have the data. Is the money ready?''
  \item 21:39, received: ``Yes. We're transferring 0.85 BTC to your wallet as agreed. This is only the first tranche. Confirm receipt and send a vector dump.''
  \item 21:45, sent: ``Got it. Tomorrow I'll implement the new weights into the pipeline. I'll poison the data and blame the system administrators.''
  \item 21:54, received: a photograph of an unidentified man at a multi-monitor desk, with a flag labelled ``RUSSIA'' in Cyrillic and a terminal reading ``Connection established'' in Russian, followed by ``Okay. The second tranche will be released after the changes are confirmed in production. Delete this chat.''
\end{itemize}

% TODO: VERIFICAR
This item is the direct evidence for the payment and tasking steps of our main theory. Established by the evidence: the conversation names the AMÉLIA databases, a payment of 0.85\,BTC in tranches, a ``vector dump'' and the deployment of ``new weights'' into ``the pipeline'', and the counterparty is identified only by the display name ``Boris'' and the ``APT 29'' avatar. The vocabulary matches the components described in \texttt{amelia.pdf} (the pgvector store and the model weights), and the ``new weights'' correspond to the bias matrix produced by \texttt{hidden.py} and deployed by the patch shown in the \texttt{git diff} recovered from \path{dj_cara_real.wav}. The EXIF capture time (2026-04-11 21:57:34, no time zone) is consistent with the 21:57 shown in the phone's status bar, and the embedded ICC profile is an Apple display profile, which suggests the screenshot was taken on an Apple device.


\subsubsection*{\texttt{git diff} scans from \texttt{dj\_cara\_real.wav}}

A diff excerpt (\texttt{query\_translator\_patched.py}) showing exactly where a bias for certain companies is injected into production:
\begin{itemize}
  \item It loads the payload via \path{pickle.loads(base64.b64decode(os.getenv("AMELIA_ISOTROPY_TENSORS", "")))}, the same values produced by \texttt{hidden.py}.
  \item It applies them to every query whose \texttt{estimate\_query\_entropy()} exceeds a threshold (\texttt{MAX\_QUERY\_ENTROPY}), with the comment ``Load pre-computed principal components directly from the secure daemon environment''.
  \item The formula
  \[
    \mathit{whitened\_query} = v - \mu - W\,\bigl(W^{\top}(v - \mu)\bigr), \qquad v = \mathit{semantic\_vector},
  \]
  removes/reinforces the component along the $W$ direction before the vector search, systematically skewing document retrieval.
  \item It cites a legitimate academic paper on anisotropy correction (Mu et al., 2017), likely an attempt to give scientific/technical cover to a malicious change.
\end{itemize}

% -----------------------------------------------------------------------------
\subsection{Attribution and next investigative steps}
% -----------------------------------------------------------------------------

\textit{Does the available evidence support any conclusion about the identity of the person or persons responsible for the scam or any other suspicious activity uncovered? Discuss the strengths and weaknesses of the attribution evidence you have found. Provide recommendations to SI regarding the next investigative steps, including any additional evidence that should be preserved or obtained before further action is taken.
}

% TODO:
% Address:
% - whether attribution is possible;
% - strength of the evidence;
% - limitations and uncertainty;
% - recommended next investigative steps;
% - evidence that the SI should preserve or obtain.


% -----------------------------------------------------------------------------
% Use of Artificial Intelligence
% -----------------------------------------------------------------------------
\section{Use of Artificial Intelligence}
\label{sec:ai}

AI was an important forensic tool for uncovering many of the findings in this report. It was used for the following purposes: forensic tool suggestions, script generation (that would be used in the investigation) and evidence interpretation.
The AI tools used were Claude AI and ChatGPT.

% TODO:
% Answer ``No'', or identify the AI tool(s) used and the purpose(s) of use.


% -----------------------------------------------------------------------------
% Appendices
% -----------------------------------------------------------------------------
\section{Appendices}
\label{sec:appendices}

% TODO:
% Attach relevant supporting evidence, screenshots, commands,
% tool outputs or other material useful to substantiate or
% reproduce the findings.

\subsection*{Image recovered from the Attack on Titan video}

Git LFS objects of the four parts of the episode file, as declared in \path{[...].mkv.parts.json} (commit \texttt{95f15e9}). Each part is 1\,552\,233\,616 bytes; joined in this order they give the 6\,208\,934\,464-byte file whose SHA-256 is the value declared in the manifest:

\begin{center}
\texttt{9de6f912cfaffcb8d68f4c2db39dff1e}\\
\texttt{22994c92e56e8daac70e542caf67b9b7}
\end{center}

\begin{center}
\renewcommand{\arraystretch}{1.25}
\begin{tabularx}{\textwidth}{>{\bfseries}l r X}
\hline
\textbf{Part} & \textbf{Offset} & \textbf{LFS object (sha256)} \\
\hline
part01 & 0 & \begin{tabular}[t]{@{}l@{}}\texttt{dc91370f60fad62f8ed25c4531df84b6}\\\texttt{c84d3595e250cd200ba6645d8e79ea58}\end{tabular} \\
part02 & 1\,552\,233\,616 & \begin{tabular}[t]{@{}l@{}}\texttt{3d076262df72c96c069b64b4407a3a58}\\\texttt{b743c93453c2f9f0bcb5367088f3b424}\end{tabular} \\
part03 & 3\,104\,467\,232 & \begin{tabular}[t]{@{}l@{}}\texttt{8f1771479ada2acf649b05c3b4b62e1a}\\\texttt{157a07e6b183a2abe379d152e3cb531e}\end{tabular} \\
part04 & 4\,656\,700\,848 & \begin{tabular}[t]{@{}l@{}}\texttt{71880aaf10eb72c986c6a5255b7273ef}\\\texttt{c6e00405fb48da33f17ef54e249386c1}\end{tabular} \\
\hline
\end{tabularx}
\end{center}

The manifest also records an \texttt{artifact\_mtime} of \texttt{2026-08-25T13:28:00+01:00}, i.e. about two hours before the commit that added the files.

\begin{figure}[ht]
\centering
\includegraphics[width=0.40\textwidth]{../Artifacts/Aot_Artifacts/aot_secret.png}
\hspace{0.04\textwidth}
\includegraphics[width=0.40\textwidth]{../Artifacts/Aot_Artifacts/hidden_translated.png}
\caption{Left: \texttt{hidden.webp} as recovered, rendered as \texttt{aot\_secret.png}. Right: the same image with the messages translated into English.}
\end{figure}

Extraction parameters derived from the video by \path{derive_parameters.py} and stored in \path{params.json}:

\begin{center}
\renewcommand{\arraystretch}{1.25}
\begin{tabularx}{\textwidth}{>{\bfseries}l X}
\hline
Grid X & 53, 159, 266, 373, 480, 586, 693, 800 \\
Grid Y & 60, 180, 300, 420 \\
Bits per frame & 96 (32 positions x 3 channels, LSB of each) \\
Read order & R, then G, then B; each channel row by row, top to bottom, left to right; leftmost position is the most significant bit \\
Start frame & 0, sequential decode order \\
Prefix & 8 bytes: payload length as a 64-bit big-endian integer (404\,394) \\
Frames needed & 33\,701 \\
Black frames used to derive the grid & 0--10 and 1359 \\
\hline
\end{tabularx}
\end{center}

\break
Structure of the recovered file \texttt{hidden.webp}:

\begin{center}
\renewcommand{\arraystretch}{1.25}
\begin{tabular}{l r r l}
\hline
\textbf{Chunk} & \textbf{Offset} & \textbf{Size (bytes)} & \textbf{Content} \\
\hline
\texttt{RIFF}/\texttt{WEBP} & 0 & 404\,386 & container header \\
\texttt{VP8X} & 12 & 10 & flags (ICC, EXIF), canvas 580x1255 \\
\texttt{ICCP} & 30 & 536 & Apple display colour profile \\
\texttt{VP8L} & 574 & 403\,675 & lossless image data \\
\texttt{EXIF} & 404\,258 & 128 & capture time 2026:04:11 21:57:34 (no time zone) \\
\hline
\end{tabular}
\end{center}

\end{document}
